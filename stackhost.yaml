user:
  name: ishikki
  github: ishikki-akabane

runtime:
  image: "python:3.11-slim" # "node:22"

commands:
  build:
    # - "pnpm install"
    # - "pnpm run build"
    - "pip install -r requirements.txt"
  package: ""
  start: "python api.py"

repository:
  branch: "main"
  auto_deploy: false
