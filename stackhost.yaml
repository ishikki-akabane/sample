user:
  name: ishikki
  github: ishikki-akabane

runtime:
  image: "node:22"

commands:
  build:
    - "pnpm install"
    - "pnpm run build"
  package: ""
  start: "pnpm run start"

repository:
  branch: "main"
  auto_deploy: false
